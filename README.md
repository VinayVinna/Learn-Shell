# Learn-Shell
Redhat-9-DevOps-Practice

what is a script ?

- script is file has some logic in execution, however it follows the path of top to down

shell script will execute the commands from top to bottom, however the lines with # character will not get executed,
those are treated as comments.

Reddirectors

1. Input redirectors (<)
2. output redirectors (>)
3. output appenders (>>)

output can comprise both output & errors, if we want both to the same file then &> will be used.
script - input from a file / output to a file

we are going to learn bash shell scripting.

following are the topics.